# RepoLine Cursor App Bridge

This local Cursor extension exposes a small Unix socket bridge so RepoLine can
submit prompts into the active Cursor composer without relying on external UI
automation.

This path is experimental and should not be the default onboarding route. It depends on an open Cursor desktop session for the target workspace and on local bridge compatibility with the current Cursor build.
