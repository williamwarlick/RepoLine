LIVEKIT_URL=
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=
LIVEKIT_AGENT_NAME=clawdbot-agent

# Recommended manual default for first-run onboarding.
BRIDGE_CLI_PROVIDER=codex
BRIDGE_WORKDIR=
# Defaults to composer-2-fast for Cursor if left empty.
BRIDGE_MODEL=
# Cursor only. Use `app` to submit into the open Cursor app and read replies from the app state, or `cli` for headless cursor-agent.
BRIDGE_CURSOR_TRANSPORT=cli
# Gemini only. Use `api` for the direct Gemini Developer API path, or `cli` for the Gemini CLI wrapper.
BRIDGE_GEMINI_TRANSPORT=cli
BRIDGE_THINKING_LEVEL=low
# readonly = inspect only, workspace-write = sandboxed project edits, owner = highest-permission local mode
BRIDGE_ACCESS_POLICY=readonly
# RepoLine loads this skill from the selected repo by default.
REPOLINE_SKILL_NAME=repoline-voice-session
# RepoLine also loads this mutable local skill for provider-specific pronunciation fixes.
REPOLINE_TTS_PRONUNCIATION_SKILL_NAME=repoline-tts-pronunciation
# Optional explicit override. Leave empty to require the installed RepoLine skill.
BRIDGE_SYSTEM_PROMPT=
BRIDGE_CHUNK_CHARS=80
BRIDGE_THINKING_SOUND_PRESET=soft-pulse
BRIDGE_THINKING_SOUND_INTERVAL_MS=1800
BRIDGE_THINKING_SOUND_VOLUME=0.11
# Keep this true to play the server-side cue only for inbound SIP/phone calls.
BRIDGE_THINKING_SOUND_SIP_ONLY=true
FINAL_TRANSCRIPT_DEBOUNCE_SECONDS=0.35
BRIDGE_SHORT_TRANSCRIPT_WORDS=2
BRIDGE_SHORT_TRANSCRIPT_DEBOUNCE_SECONDS=0.55
BRIDGE_TELEMETRY_JSONL=./logs/bridge-telemetry.jsonl

BRIDGE_STT_PROVIDER=livekit
LIVEKIT_STT_MODEL=deepgram/nova-3
LIVEKIT_STT_LANGUAGE=multi
GEMINI_API_KEY=
GOOGLE_API_KEY=
DEEPGRAM_API_KEY=
DEEPGRAM_STT_MODEL=nova-3
LIVEKIT_TURN_ENDPOINTING_MODE=dynamic
LIVEKIT_TURN_MIN_ENDPOINTING_DELAY_SECONDS=0.35
LIVEKIT_TURN_MAX_ENDPOINTING_DELAY_SECONDS=1.4
LIVEKIT_TURN_INTERRUPTION_MODE=adaptive
LIVEKIT_FALSE_INTERRUPTION_TIMEOUT_SECONDS=1.5
LIVEKIT_RESUME_FALSE_INTERRUPTION=true
BRIDGE_TTS_PROVIDER=livekit
LIVEKIT_TTS_MODEL=cartesia/sonic-3
LIVEKIT_TTS_VOICE=9626c31c-bec5-4cca-baa8-f8ba9e84c8bc
ELEVENLABS_API_KEY=
ELEVENLABS_TTS_MODEL=eleven_flash_v2_5
ELEVENLABS_VOICE_ID=onwK4e9ZLuTAKqWW03F9
LIVEKIT_RECORD_AUDIO=false
LIVEKIT_RECORD_TRACES=false
LIVEKIT_RECORD_LOGS=false
LIVEKIT_RECORD_TRANSCRIPT=false
BRIDGE_PROMETHEUS_PORT=

BRIDGE_GREETING=RepoLine is live. What do you want to work on?
